### I am planning to use ninja build this time.
cmake -G Ninja .. (instead of cmake ..)

### can use "ninja" instead of "make -j${nproc}" as ninja in multithreaded by design.
### Alternatively we can use "cmake -build .SSSS"
