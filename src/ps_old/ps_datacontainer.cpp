#include <ps_datacontainer.hpp>

namespace ps
{
    DataContainer::DataContainer(Type& type, const void* data_ptr, size_t offset_bytes, size_t length_bytes)
    {

    }

    bool DataContainer::allocate(Type type, size_t size)
    {

    }

    void deallocate()
    {
        
    }
} // namespace ps
